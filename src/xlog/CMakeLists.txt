file(GLOB SUB_HEADERS ${HEADERS_FLAG})
source_group("xlog" FILES ${SUB_HEADERS})

get_property(CURRENT_HEADERS GLOBAL PROPERTY _HEADERS)
list(APPEND CURRENT_HEADERS ${SUB_HEADERS})
set_property(GLOBAL PROPERTY _HEADERS ${CURRENT_HEADERS})

message (STATUS "---------[CMAKE_CURRENT_SOURCE_DIR]:" ${CMAKE_CURRENT_SOURCE_DIR})
file(GLOB SUB_SOURCES ${SOURCES_FLAG})
source_group("xlog" FILES ${SUB_SOURCES})

get_property(CURRENT_SOURCES GLOBAL PROPERTY _SOURCES)
list(APPEND CURRENT_SOURCES ${SUB_SOURCES})
set_property(GLOBAL PROPERTY _SOURCES ${CURRENT_SOURCES})